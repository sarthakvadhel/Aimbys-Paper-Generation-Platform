@model Aimbys.Web.ViewModels.Questions.QuestionIndexViewModel
@{
    ViewData["Title"] = "Question Bank";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="mb-0">Question Bank (Institute)</h4>
    <a asp-action="Import" class="btn btn-outline-primary">
        <i class="bi bi-upload me-1"></i>Import Questions
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        @if (!Model.Questions.Any())
        {
            <p class="text-muted text-center py-4">No questions in the bank yet.</p>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead>
                        <tr>
                            <th>Type</th>
                            <th>Subject</th>
                            <th>Difficulty</th>
                            <th>Marks</th>
                            <th>Status</th>
                            <th>Created</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var q in Model.Questions)
                        {
                            <tr>
                                <td><span class="badge bg-secondary">@q.Type</span></td>
                                <td>@q.SubjectName</td>
                                <td>@q.Difficulty</td>
                                <td>@q.Marks</td>
                                <td><span class="badge bg-info">@q.Status</span></td>
                                <td>@q.CreatedAtUtc.ToString("dd MMM yyyy")</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
